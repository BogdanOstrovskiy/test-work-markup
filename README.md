# test-work-markup

> My test project

## Build Setup

``` bash
# install dependencies
$ yarn install
$ npm install

# serve with hot reload at localhost:3000
$ gulp

# build for production and launch server
$ gulp build

```
